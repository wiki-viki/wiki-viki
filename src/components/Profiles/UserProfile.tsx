import React from 'react';

const UserProfile = ({ city, mbti, job, sns, birthday, nickname, bloodType, nationality }) => {
  return (
    <section className="profile-shadow h-[126px] w-full rounded-10 p-4">
      <div className=""></div>
    </section>
  );
};

export default UserProfile;
